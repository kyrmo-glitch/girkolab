## Отчет по лабораторной работе №2

### №1

**Задание**

Тимофей составляет 5-буквенные коды из букв Т, И, М, О, Ф, Е, Й. Буква Й может использоваться в коде не более одного раза, при этом она не может стоять на первом месте, на последнем месте и рядом с буквой И. Все остальные буквы могут встречаться произвольное количество раз или не встречаться совсем. Сколько различных кодов может составить Тимофей?

```python
from itertools import product

def countwords():
    letters = "ТИМОФЕЙ"
    count = 0
    for x in product(letters, repeat=5):
        s = ''.join(x)
        if (s.count('Й') <= 1 and s[0] != 'Й' and s[-1] != 'Й' and 'ИЙ' not in s and 'ЙИ' not in s):
            count += 1
    
    return count


print(countwords())
```

**Результат**

<img width="615" height="30" alt="изображение" src="https://github.com/user-attachments/assets/ac01c67a-0f24-4c04-8a41-451cea122848" />



**Описание проделанной работы**

Цикл for x in product(letters, repeat=5) генерирует все возможные варианты слов длиной 5 из букв слова "ТИМОФЕЙ". Получаются кортежи вида: ('Т', 'И', 'М', 'О', 'Ф', 'Е', 'Й'), с помощью s = ''.join(x) перевожу это в строку. Расписываю все условия если выполняются увеличиваю счетчик на 1.

---
### №2

**Задание**

Сколько единиц содержится в двоичной записи значения выражения  $$( 4 ^ {2020} + 2 ^ {2017} − 15 )$$? 

```python
def countone():
    x = 4**2020 + 2**2017 - 15
    count = 0
    while x > 0:
        if x % 2 == 1:  
            count += 1
        x = x // 2           
    return count

print(countone())
```

**Результат**

<img width="619" height="30" alt="изображение" src="https://github.com/user-attachments/assets/f99df677-0ff2-4fe9-bd1d-a5721596ce45" />


**Описание проделанной работы**

Пока число x не будет равно 0 у меня будет выполняться перевод x в двоичное число и считать количество единиц. Возвращает количество единиц.

---
### №3

**Задание**

Найдите среди целых чисел, принадлежащих числовому отрезку \( [174457; 174505] \), числа, имеющие ровно два различных натуральных делителя, не считая единицы и самого числа. Для каждого найденного числа запишите эти два делителя в два соседних столбца на экране с новой строки в порядке возрастания произведения этих двух делителей. Делители в строке также должны следовать в порядке возрастания. Например, в диапазоне \( [5; 9] \) ровно два различных натуральных делителя имеют числа 6 и 8, поэтому для этого диапазона вывод на экран должен содержать следующие значения:
```
  2 3
  2 4
```


```python
def searchediv(n):
    div = set()
    for i in range(2, int(n**0.5) + 1):
        if n % i == 0:
            div.add(i)
            div.add(n//i)
        if len(div) > 2:
            break
    return div

for i in range(174457, 174506):
    d = searchediv(i)
    if len(d) == 2:
        print(d[0], d[1])
```

**Результат**

<img width="627" height="134" alt="изображение" src="https://github.com/user-attachments/assets/06e8ac65-6d94-49b1-88a0-74f5e904da6d" />


**Описание проделанной работы**

Создаю множество для хранения найденных делителей и сразу исключаю дубикаты. Перебираю i от 2 по условию (не считая единицы и самого числа).
Добавляю в множество первый делитель полученный в результате деления с отстатком(n % i == 0) и делитель полученный в результате целочисленного деления (n//i). Если делителей больше 2 заканчиваю перебор (имеющие ровно два различных натуральных делителя). Возращаю множество делителей. Перебираю числа от 174457 до 174505. Сортирую множество и использую функциию для поиска делителей. Если делителей 2, вывожу первый и второй.

---

## Использованные источники

[itertools— Функции, создающие итераторы для эффективного зацикливания](https://docs.python.org/3/library/itertools.html)




  




